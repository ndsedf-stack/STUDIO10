# Fix TypeScript errors in App.tsx

This PR fixes TypeScript errors in App.tsx by adding proper type annotations:

- Added type annotation to selectedDay state (string)
- Added type annotation to handleWeekChange function parameter (number)
- All existing functionality preserved including workout display and day selection